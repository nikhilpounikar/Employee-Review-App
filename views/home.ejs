<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="css/home.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ERS</title>
</head>
<body>
    <%if(locals.user){%>
        <div class="outerDiv">    
            <% if(!locals.user.isAdmin){ %>
                <div class="adminPass">
                    <form action="/users/login-as-admin/<%= user._id %>" method="post">
                        <input name="adminPassCode" type="password" placeholder="Password">
                        <button type="submit" class="submitHomeButton"> Log In as Admin</button>
                    </form>
                </div>
            <%}%>    
        

                <!-- the text part -->
        <div class="home-box">
            <i class="text-colr">Welcome to Employee Review System that helps the admin identify top-performing employees by assigning co-worker reviews. It also allows the admin to promote employees to admin status and add new employees. This system creates a culture of accountability and promotes excellence, leading to better hiring decisions and a stronger workforce.</i>
            <hr>
        </div>

        <div class="home-box">
            <div class="employeereview">Kindly, review your co-employees<br>
            </div>

            <div class="reviewBox">
                <% for(let userWithPendingReview of user.usersWithPendingReviews) {%>
                   
                        <div class="outerBox">
                            <h6>
                                To : <%= userWithPendingReview.name%>
                            </h6>
                            <div class="innerBox">
                                <form action="/reviews/newReview/<%= userWithPendingReview._id %>" method="get">
                                    <textarea class="formClass" placeholder="Review, here !" name="newReview" rows="5"
                                    id="floatingTextarea2">
        
                                    </textarea>
                                    <br>
                                    <button type="submit" class="submitHomeButton"> Submit </button>
                                </form>
                            </div>
                        </div>
        
                <%}%>   
            </div>
        </div>

        <div class="home-box">
           <div class="myreview">My Reviews <img src="../images/rating.webp"></div>
            <div class="reviewBox">

            
                <div class="card-out">
                    <% for(let review of user.givenReviews ) {%>
                        <div class="in-card">
                            <h5 class="cls-txt">Message : <%= review.content %> </h5>
                            <p class="cls-txt">To : <%= review.name %> </p>
                            
                        </div>
                    <%}%>    
                </div>
            

        </div>

    </div>
    <%}%>

</body>
</html>